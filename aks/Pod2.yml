apiVersion: v1
kind: Pod
metadata:
  name: myapp2
  labels:
    app.kubernetes.io/name: myapp2
spec:
  containers:
  - name: myapp
    image: sonambewafaa/fbclone:v2
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 91
